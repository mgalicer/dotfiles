# New computer setup
### bash
- Download .bash_profile to home directory
- Download `git autocomplete` script to home directory 
```
curl https://raw.githubusercontent.com/git/git/master/contrib/completion/git-completion.bash -o ~/.git-completion.bash
```

### Mac specific
- Speed up key repeat speed
- Turn off Siri
- (post Catalina) change default shell scripting zsh -> bash

### Browser
- Download firefox
- Download and log into 1Password
- Set everything to dark mode 

### Programming dependencies 
- git
- Node 
- Pip
- Docker 
